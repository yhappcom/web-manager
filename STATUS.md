# MintTap Web Manager Status

Operating state: **ACTIVE — CONTINUOUS EXPERT MAINTENANCE / APPLICATION + FIVE-TRACK COORDINATION + PWA SPECIALIZATION**  
Last sync: 2026-09-20

## Operating model
GitHub is canonical memory. Sequential curriculum Stages 1–12 are complete. Curriculum completion is not production certification. Detailed guards and studies live in `research/README.md` and numbered research artifacts.

## Continuous expert maintenance
083–170 — **PASS at recorded generic gates.**  
171 PWA Delegation Provenance Portability & Cross-System Federation — **PASS (generic) / PRODUCT + FEDERATION + IDP + EMPLOYER/AIRLINE + REVOCATION + MANAGED-IPAD + LEGAL + HUMAN/AT VALIDATION OPEN**.  
172 PWA Federation Trust Metadata Currentness, Split-View & Successor-Anchor Governance — **PASS (generic) / PRODUCT + FEDERATION + IDP + TRUST-ANCHOR + MANAGED-IPAD + PROVIDER + HUMAN/AT VALIDATION OPEN**.  
173 PWA Federation Trust-Policy Distribution, Rollback Resistance & Multi-Region/Offline Convergence — **PASS (generic) / PRODUCT + FEDERATION + MULTI-REGION + PROVIDER + MANAGED-IPAD + RUNTIME + HUMAN/AT VALIDATION OPEN**.

## 171–173 integrated checkpoint
- Federation is bounded issuer-specific evidence, not evergreen local delegation. External assertion, external relationship evidence, local authority mapping, local delegation generation, browser/session/token state and offline observation remain distinct.
- Federation metadata, JWKS, trust chains and local trust-anchor configuration are versioned security state. HTTPS reachability or a valid signature alone does not establish current semantic trust.
- Trust-policy publication is a distributed security-state transition: authoring, authorization, publication, regional admission, enforcement, client observation and convergence evidence are distinct.
- HTTP cache freshness/revalidation is not trust-policy semantic lifetime. RFC 9111 invalidation is path-local and does not prove global convergence.
- Planned rollout, emergency revocation and compromise recovery are separate protocols. Emergency revoke requires a monotonic security floor; unreachable enforcement domains are `currentness unknown`, not implicitly safe.
- Rollback/PITR/backup restore cannot silently lower the current trust floor. Restored components reconcile before consequence-bearing traffic resumes.
- Convergence claims are bounded to controlled enforcement domains. Client telemetry/cache state is observation, not an authority oracle or universal fleet proof.
- Long-offline PWA clients preserve unique local records/drafts but queued consequence-bearing work is re-evaluated against current server-side trust/authority floor before replay.
- Service Worker/app-shell convergence and semantic authorization-policy convergence remain separate.
- No generic claim is made that iPadOS refreshes federation metadata, keys, tokens, policy or Service Worker in the background. Physical Safari/Home Screen/MDM transfer remains OPEN.

## Persistent guards added by 171–173
`federated login PASS ≠ local delegation PASS`.  
`issuer trusted for authentication ≠ issuer trusted for delegation`.  
`token cached ≠ authority evergreen`.  
`HTTPS fetch succeeded ≠ metadata semantically trusted`.  
`metadata signature valid ≠ expected issuer bound`.  
`issuer bound ≠ resource bound`.  
`JWKS fetched ≠ key current`.  
`trust chain validates ≠ local policy accepts this chain`.  
`one valid chain ≠ no competing valid chain exists`.  
`policy authored ≠ policy authorized ≠ policy published`.  
`policy published ≠ every region admitted/enforced it`.  
`HTTP cache fresh ≠ trust policy semantically current`.  
`cache invalidated ≠ global convergence`.  
`client observed generation G ≠ server currently enforces G`.  
`all sampled clients current ≠ entire fleet converged`.  
`rollback technically possible ≠ rollback semantically authorized`.  
`emergency revoke published ≠ revoke effective everywhere`.  
`Service Worker updated ≠ trust-policy reconciliation complete`.  
`offline trust view internally consistent ≠ online authority current`.  
`remote authority unavailable ≠ unique local operational data should be destroyed`.

## Five-track state
- **A Platform/Browser:** high dependency supplier; owns HTTPS/discovery/redirect/cache/session/SW/storage/reconnect mechanics. HTTP cache and browser possession cannot establish current business authority or global policy convergence.
- **B UX/IA/Content:** very high dependency pressure; owns refresh-required, provider/control-plane unavailable, local-work-preserved and submission-paused semantics while consuming Design Studio evidence.
- **C Performance/Accessibility/Quality:** high dependency pressure; owns 171's 128-case, 172's 136-case and 173's 144-case destructive campaigns plus independent browser/device/AT/human validation.
- **D Search/Discovery/Analytics:** bounded consumer; federation/trust/convergence identifiers must not become durable acquisition identity graphs.
- **E Architecture/Security/Operations:** highest-risk owner; 171–173 close generic federation evidence, metadata/currentness and trust-policy distribution/convergence boundaries. Availability/fail-safe degradation is the adjacent bottleneck.

## Cross-repository evidence
Design Studio `progress/WEB_STATUS.md` is **W120 EXECUTABLE CROSS-CONCEPT STRESS PACKET; Stage 1 PASS / Stage 2 PASS / Stage 3 PRACTICE / NOT PASSED**. Exact-source analyze/golden/runtime closure, independent browser/native, physical-device/PWA, field Core Web Vitals, full WCAG, screen-reader and human UX evidence remain OPEN.

Software Engineering evidence remains bounded transfer evidence only. Existing generic Chromium Service Worker/offline evidence does not prove Safari/iPadOS/EFB, canonical LogMate runtime, physical connectivity-loss, storage eviction, multi-region federation or production policy convergence behavior.

## Production OPEN register
Actual `minttap.app` and LogMate-like production facts remain OPEN unless verified from canonical project/runtime evidence. This includes managed-iPad/WebKit/MDM/Shared-iPad behavior; PWA storage/update/background/sync/push; authentication/session/offline authorization; actual account/tenant/IdP/federation/issuer/JWKS/trust-anchor model; trust-policy representation/signing/control plane/regions; representative/delegation/employer/airline authority; support/adjudication roles; offline queue/replay/conflict policy; account/device/key/trust recovery; API/schema/policy/decision/reason generations; record/operation/device/effect identity; provenance/export/import/checkpoint/crypto hierarchy; provider replication/restore/backup semantics; closure/deletion evidence; retention/deletion/legal obligations; explanation/redress rights; external-recipient correction; Service Worker trust; actual RTO/RPO/MTD and emergency propagation objectives; and real physical-device/security/privacy/legal/aviation validation.

## Next learning mode
Highest-value adjacent generic work is **PWA federation trust-policy availability, emergency fail-safe degradation & recovery objectives**: determine consequence-specific behavior when provider/control-plane/region currentness cannot be established, bound stale-read/local-work capability without converting stale trust into indefinite mutation authority, and define measurable recovery/convergence objectives without inventing product SLAs.

## Persistence state
- Stages 1–12: COMPLETE at defined curriculum gates.
- Continuous maintenance: **083–173 PASS** at generic gates.
- Product/device/AT/security/privacy/legal/aviation/provider/data-model/backend/crypto/backup/compromise/recovery/evidence/policy-runtime/deletion/retention/redress/identity/notification/delegation/federation runtime validation remains OPEN.
- Reporting remains coarse/checkpoint-based.