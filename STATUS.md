# MintTap Web Manager Status

Operating state: **ACTIVE — CONTINUOUS EXPERT MAINTENANCE / APPLICATION + FIVE-TRACK COORDINATION + PWA SPECIALIZATION**  
Last sync: 2026-09-17  
Domain: `minttap.app`  
Platforms: iOS / App Store, Android / Google Play, strategic PWA/Web App capability

## Operating model
GitHub is canonical memory. Sequential curriculum Stages 1–12 are complete. Future work is targeted expert maintenance/application selected from live evidence, change-watch, risk and specialist dependencies. Curriculum completion is not production certification.

# Continuous expert maintenance checkpoints
083 — **PWA Storage Durability & Service-Worker Standards Change Watch — PASS.**  
084 — **iOS/iPadOS PWA Install, Background & Authentication Reality — PASS.**  
085 — **PWA Irreplaceable Data Recovery, Offline Authorization & Device-Loss Security — PASS.**  
086 — **PWA Direct Transport, Discovery, Pairing & Security Boundaries — PASS (generic) / PRODUCT FEASIBILITY OPEN.**  
087 — **PWA WebRTC Establishment, Managed-Network Reality & Fallback Experiment Contract — PASS (generic) / PRODUCT FEASIBILITY OPEN.**  
088 — **PWA Offline Navigation, Service-Worker Update Recovery & Observability — PASS (generic) / PRODUCT VALIDATION OPEN.**  
089 — **PWA Testing, Diagnostics & Release Evidence Architecture — PASS (generic) / TARGET-DEVICE EXECUTION OPEN.**

## 089 material findings
- PWA validation now uses four evidence tiers: deterministic logic/protocol tests → browser automation → engine/device diagnostics → exact managed-device product acceptance;
- Playwright service-worker inspection/routing is currently Chromium-only, so Chromium automation cannot establish Safari/WebKit worker correctness;
- disabling service workers can simplify network mocking but changes the product path and therefore serves only as a control test;
- WebKit separates WebContent, Network and Storage processes and provides service-worker/Web Inspector diagnostics, but simulator/inspector evidence still does not equal managed-iPad acceptance;
- release acceptance is based on state/data invariants, not merely rendered pages or HTTP 200 responses;
- canonical fault matrix now covers offline/deep-link, blackhole, partial failure, worker/shell/schema generation mismatch, interrupted migration, pending outbox, lost ACK/duplicate retry, skipped versions, storage pressure, network change, rollback and inaccessible degraded states;
- diagnostic bundles should expose structural version/state metadata while excluding flight-record contents and unnecessary stable identifiers;
- `automated browser PASS ≠ Safari PASS ≠ physical iPad PASS ≠ managed-EFB product PASS`;
- absence of telemetry from a broken/offline client cannot be counted as health evidence.

# Persistent PWA guards
`public website ≠ installable web experience ≠ offline-capable task ≠ synchronized product`.
`online signal ≠ usable network ≠ successful navigation ≠ task-ready application`.
`local save ≠ persistent storage ≠ sync queued ≠ transport attempt ≠ remote acknowledgement ≠ reconciliation ≠ backup ≠ tested restore`.
`worker update found ≠ installed ≠ activated ≠ controlling ≠ application-compatible`.
`automated browser PASS ≠ Safari PASS ≠ physical iPad PASS ≠ managed-EFB product PASS`.
`rendered/HTTP 200 ≠ PWA state/data invariant satisfied`.
`navigation preload ≠ offline fallback ≠ cache correctness`.
`reconstructible cache ≠ authoritative user records/outbox`.
`Home Screen installed ≠ background synchronization available`.
`push event execution ≠ arbitrary background execution`.
`previously authenticated ≠ currently server-authorized ≠ authorized indefinitely offline`.
`HTTPS + same-origin storage ≠ complete device-loss/XSS/data-at-rest security`.
`Web Crypto available ≠ safe key-management/recovery architecture established`.
`WebSocket/WebTransport available ≠ peer discovery`.
`WebRTC P2P capability ≠ zero-infrastructure discovery ≠ unattended pairing ≠ background execution`.
`paired identity ≠ current address ≠ current ICE candidates ≠ signaling delivered`.
`STUN reachable ≠ peer reachable`; `TURN success ≠ direct P2P`.
`same LAN ≠ direct reachability`.
`origin deployment complete ≠ all installed clients updated`.
`origin rollback complete ≠ installed client recovered`.

# Five-track state
All tracks have integrated foundation/practitioner coverage; allocation is risk/evidence-gap driven.
- **A Platform/Browser:** strong; supplies worker/cache/storage/network mechanics and engine-specific evidence boundaries. Current standards/WebKit details remain CHANGE WATCH.
- **B UX/IA/Content:** consumes truthful local-only/pending/conflict/degraded/update/recovery state semantics; visual treatment remains Design Studio-owned.
- **C Performance/Accessibility/Quality:** current highest-value generic bottleneck addressed by 089. Owns the four-tier validation architecture, fault injection, accessibility assertions and evidence labeling. Exact Safari/AT/physical-device execution remains OPEN.
- **D Search/Discovery/Analytics:** consumes privacy-minimized structural health signals; missing offline telemetry and analytics cannot certify durability/recovery.
- **E Architecture/Security/Operations:** consumes release invariants, diagnostic minimization, lineage and rollback/forward-fix evidence; product closure still requires implementation/device evidence.

# Cross-repository evidence
Design Studio Web Design checked 2026-09-17: Stage 1/2 PASS, Stage 3 PRACTICE / NOT PASSED; W039 bounded correction-chain contract ready but execution OPEN. No Safari, cross-browser, screen-reader, physical-device, field-CWV or human-UX PASS may be inferred.

Software Engineering remains implementation owner for executable fixtures, browser/device tests, fault injection, schema/outbox recovery and retained raw artifacts. Web Manager specifies evidence/acceptance contracts rather than choosing its implementation framework.

# Production OPEN register
Actual `minttap.app` production state remains OPEN unless verified from project evidence.

PWA/EFB OPEN includes exact managed-iPad OS/WebKit/MDM policy, Home Screen retention, worker/cache behavior, offline/deep-link navigation, update waiting/activation/takeover, schema migration/interruption, long-offline skipped-version recovery, storage pressure, accessible degraded/recovery UX, network isolation/VPN/proxy/content filtering, signaling/ICE/TURN behavior, pairing credential persistence/revocation, foreground/background/suspension behavior, LogMate outbox/protocol, independent backup/restore, direct unattended PWA↔native transport, Safari remote-inspection/diagnostic permissions and privacy approval for production diagnostics.

# Next learning mode
Generic testing/diagnostic architecture is now sufficient for implementation handoff. Without new runtime evidence, avoid writing another testing primer. Highest-value autonomous work should examine **release acceptance and support/incident evidence across privacy-safe diagnostics, accessibility/degraded-state verification and exact Safari/iPad execution constraints**, or consume new Software Engineering/Design Studio artifacts when they appear. Continue Service Worker/WebKit/manifest capability CHANGE WATCH.

# Persistence state
- Stages 1–12: COMPLETE at defined curriculum gates.
- Continuous maintenance: **083–089 PASS**.
- PWA generic direct-sync, offline/update/recovery and testing/diagnostic architectures: sufficient for implementation handoff; product/device validation OPEN.
- Reporting remains coarse/checkpoint-based.
